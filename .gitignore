# Run artefacts: prompts, event streams, agent logs, captured diffs, and the
# per-repo state file. All of it contains task and PR content, so none of it
# belongs in the repo.
runner/work/

# The task checkouts themselves live beside run.py and are never committed here.
pr-*/
[0-9][0-9][0-9][0-9]/

__pycache__/
*.pyc
.DS_Store
.venv/
